"use client";

import { createContext, useContext, useState } from "react";

type DashboardContents = {
  displayLocation: string;
  adm4: string;
  latitude: string;
  longitude: string;
};

export default function dashContext() {}
